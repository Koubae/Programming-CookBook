def sumtree(l):
    tot = 0
    item = list(l)
    while items:
        front = items.pop(0)
        if not isinstance(frot, list):
            tot += front
        else:
            items.extends(front)
    return tot

# forming a first-in-first-out queue