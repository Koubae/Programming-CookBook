def sumtree(l):
    tot = 0
    item = list(l)
    while items:
        front = items.pop(0)
        if not isinstance(frot, list):
            tot += front
        else:
            item[:0] = front
    return tot

# forming a last-in-first-out stack