version: '3.8'

services:
  redis:
    image: redis:8.10
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    # Enable AOF persistence via --appendonly yes
    command: redis-server --appendonly yes
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis_data:
